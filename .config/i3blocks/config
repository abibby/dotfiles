
# Helpful links
# FontAwesome cheatsheet https://fontawesome.com/cheatsheet

[zlist]
command=printf '💾%s' $(ssh frank-wan "zpool list -o name,cap,health" | grep volume1 | awk '{ print $3="ONLINE" ? "OK" : $3 }')
interval=3600
color=#F79494

# Guess the weather hourly
[weather]
# github.com/zwzn/weather
command=echo ⛅$($HOME/go/bin/weather)
interval=3600
color=#A4C2F4

# Query my default IP address only on startup
# [ip]
# command=hostname --ip-addresses | awk '{ print "IP:" $1 }'
# interval=once
# color=#91E78B

# Update time every 5 seconds
[time]
command=date +'%a %-e %-I:%M:%S %P'
interval=1
